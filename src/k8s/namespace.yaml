apiVersion: v1
kind: Namespace
metadata:
  name: doa-production
  labels:
    environment: production
    team: doa-engineering
---
# ⚠️ No network policies defined — all pods can communicate with everything
# ⚠️ No resource quotas — risk of runaway resource consumption
# ⚠️ No pod security policies — containers run as root by default
